# 📸 HƯỚNG DẪN CÓ HÌNH ẢNH: START & DEBUG VISUAL STUDIO 2022

---

## 🎯 PHẦN 1: START PROJECT

### 🖼️ BƯỚC 1: MỞ SOLUTION

**Nhìn thấy gì:**
```
┌─────────────────────────────────────────────┐
│ Visual Studio 2022                      [_][□][x] │
├─────────────────────────────────────────────┤
│ File  Edit  View  Git  Project  Build      │
├─────────────────────────────────────────────┤
│                                             │
│    📂 Open a project or solution            │
│    📄 Open a local folder                   │
│    🆕 Create a new project                  │
│                                             │
└─────────────────────────────────────────────┘
```

**Làm gì:**
1. Click **"Open a project or solution"**
2. Navigate: `D:\University\Semester7\Cloud\Project\SimplCommerce`
3. Chọn: **`SimplCommerce.sln`**
4. Click: **[Open]**

---

### 🖼️ BƯỚC 2: ĐỢI RESTORE PACKAGES

**Nhìn thấy gì:**
```
┌─────────────────────────────────────────────────────────┐
│ Solution Explorer - SimplCommerce (XX projects)         │
├─────────────────────────────────────────────────────────┤
│ ▼ Solution 'SimplCommerce' (XX of XX projects)         │
│   ▶ src                                                │
│   ▶ test                                               │
│   📄 SimplCommerce.sln                                  │
└─────────────────────────────────────────────────────────┘

Thanh status dưới cùng:
┌─────────────────────────────────────────────────────────┐
│ ⚙️ Restoring NuGet packages...        Ready    Ln 1  │
└─────────────────────────────────────────────────────────┘
```

**Làm gì:**
- **ĐỨNG YÊN** - đợi cho đến khi thấy: ✅ **"Ready"**
- Có thể mất 2-5 phút lần đầu

---

### 🖼️ BƯỚC 3: SET STARTUP PROJECT

**Nhìn thấy gì:**
```
┌─────────────────────────────────────────────────────────┐
│ Solution Explorer                                       │
├─────────────────────────────────────────────────────────┤
│ ▼ Solution 'SimplCommerce' (XX of XX projects)         │
│   ▼ src                                                │
│     ▶ Database                                         │
│     ▶ Modules                                          │
│     ▶ SimplCommerce.Infrastructure                     │
│     ▶ SimplCommerce.Module.Core                        │
│     📁 SimplCommerce.WebHost  ← CLICK VÀO ĐÂY!        │
└─────────────────────────────────────────────────────────┘
```

**Làm gì:**
1. **Right-click** vào `SimplCommerce.WebHost`
2. Menu hiện ra:
   ```
   ┌────────────────────────────────┐
   │ Build                          │
   │ Rebuild                        │
   │ Clean                          │
   │ ─────────────────────────────  │
   │ ⭐ Set as Startup Project      │ ← CLICK!
   │ ─────────────────────────────  │
   │ Manage NuGet Packages...       │
   │ Properties                     │
   └────────────────────────────────┘
   ```
3. Chọn: **"Set as Startup Project"**

**Kết quả:**
```
📁 SimplCommerce.WebHost  ← Bây giờ IN ĐẬM (BOLD)
```

---

### 🖼️ BƯỚC 4: CHỌN DEBUG/START MODE

**Nhìn thấy gì trên Toolbar:**
```
┌─────────────────────────────────────────────────────────┐
│ File  Edit  View  Git  Project  Build  Debug           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ [SimplCommerce.WebHost ▼] [▶️ Start] [🔧 Attach]       │
│         (1)                    (2)       (3)            │
│                                                         │
│ Any CPU ▼  Debug ▼                                     │
│   (4)       (5)                                        │
└─────────────────────────────────────────────────────────┘

(1) = Project selector (đã chọn SimplCommerce.WebHost)
(2) = Start button (màu xanh lá)
(3) = Attach to process
(4) = Platform (để Any CPU)
(5) = Configuration (chọn Debug hoặc Release)
```

**Làm gì:**
- **Không cần thay đổi gì**, mặc định OK

---

### 🖼️ BƯỚC 5: START PROJECT!

**Option A: Start WITHOUT Debug (Nhanh hơn)**
```
⌨️ Nhấn: Ctrl + F5
```

**Option B: Start WITH Debug**
```
⌨️ Nhấn: F5
```

**Option C: Click Button**
```
Click nút: [▶️ Start] (hoặc [▶️ SimplCommerce.WebHost])
```

---

### 🖼️ BƯỚC 6: XEM KẾT QUẢ

**Console/Output hiện ra:**
```
┌─────────────────────────────────────────────────────────┐
│ info: Microsoft.Hosting.Lifetime[14]                    │
│       Now listening on: https://localhost:49206         │
│ info: Microsoft.Hosting.Lifetime[14]                    │
│       Now listening on: http://localhost:49208          │
│ info: Microsoft.Hosting.Lifetime[0]                     │
│       Application started. Press Ctrl+C to shut down.   │
└─────────────────────────────────────────────────────────┘
```

**Browser tự động mở:**
```
🌐 https://localhost:49206

┌─────────────────────────────────────────────────────────┐
│ 🔒 https://localhost:49206            [Search] [Profile] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│         🛒 SIMPLCOMMERCE                               │
│         ─────────────────                              │
│                                                         │
│   [Home] [Products] [Categories] [About] [Contact]     │
│                                                         │
│   ┌───────────────────────────────────────────┐       │
│   │  Welcome to SimplCommerce                 │       │
│   │  Your E-Commerce Platform                 │       │
│   └───────────────────────────────────────────┘       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

✅ **THÀNH CÔNG!** Website đang chạy!

---

## 🐞 PHẦN 2: DEBUG PROJECT

### 🖼️ BƯỚC 1: MỞ FILE CODE

**Ví dụ mở HomeController:**

```
Solution Explorer:
▼ SimplCommerce.WebHost
  ▼ Controllers
    📄 HomeController.cs  ← Double-click
```

**Code hiện ra:**
```csharp
     1  using Microsoft.AspNetCore.Mvc;
     2  
     3  namespace SimplCommerce.WebHost.Controllers
     4  {
     5      public class HomeController : Controller
     6      {
     7          public IActionResult Index()
     8          {
     9              var data = "Hello SimplCommerce";  ← SET BREAKPOINT Ở ĐÂY
    10              return View();
    11          }
    12      }
    13  }
```

---

### 🖼️ BƯỚC 2: SET BREAKPOINT

**Làm gì:**
1. Click vào **VIỀN TRÁI** (left margin) của dòng 9
2. Chấm đỏ xuất hiện: 🔴

**Nhìn thấy gì:**
```csharp
     7          public IActionResult Index()
     8          {
🔴   9              var data = "Hello SimplCommerce";
    10              return View();
    11          }
```

**Hotkey:**
- Đặt cursor tại dòng → nhấn **F9** → toggle breakpoint

---

### 🖼️ BƯỚC 3: START DEBUG

```
⌨️ Nhấn: F5
```

**Browser mở, access homepage → Code PAUSE tại breakpoint:**

```csharp
┌─────────────────────────────────────────────────────────┐
│ HomeController.cs                               [x]     │
├─────────────────────────────────────────────────────────┤
│      7          public IActionResult Index()            │
│      8          {                                       │
│ ➡️🔴 9              var data = "Hello SimplCommerce";    │ ← DỪNG Ở ĐÂY
│     10              return View();                      │
│     11          }                                       │
└─────────────────────────────────────────────────────────┘

Toolbar thay đổi:
┌─────────────────────────────────────────────────────────┐
│ [⏸️ Break] [▶️ Continue] [⏹️ Stop] [🔄 Restart]         │
│             (F5)         (Shift+F5) (Ctrl+Shift+F5)     │
│                                                         │
│ [⤵️ Step Into] [⤴️ Step Over] [⤴️ Step Out]            │
│    (F11)         (F10)           (Shift+F11)           │
└─────────────────────────────────────────────────────────┘
```

---

### 🖼️ BƯỚC 4: XEM VARIABLES

**Locals Window tự động hiện:**
```
┌─────────────────────────────────────────────────────────┐
│ Locals                                    [Watch] [Autos] │
├─────────────────────────────────────────────────────────┤
│ Name              Value                    Type          │
├─────────────────────────────────────────────────────────┤
│ ▶ this            {HomeController}         HomeController│
│ ▶ HttpContext     {...}                    HttpContext   │
│   data            "Hello SimplCommerce"    string        │ ← Giá trị của biến
└─────────────────────────────────────────────────────────┘
```

**Hover mouse qua biến:**
```csharp
🔴 9  var data = "Hello SimplCommerce";
           ^^^^
           └─ Hover → Popup hiện:
              ┌──────────────────────┐
              │ data = "Hello..."    │
              │ string               │
              └──────────────────────┘
```

---

### 🖼️ BƯỚC 5: NAVIGATE CODE

**F10 - Step Over (chạy dòng hiện tại):**
```csharp
     8          {
🔴   9              var data = "Hello SimplCommerce";  ← Đang ở đây
    10              return View();
    
    ⌨️ Nhấn F10 →
    
     8          {
     9              var data = "Hello SimplCommerce";
🔴  10              return View();  ← Chuyển xuống dòng này
```

**F11 - Step Into (vào bên trong method):**
```csharp
🔴  10              return View();  ← Nhấn F11
    
    → Code nhảy vào source code của View() method
```

**F5 - Continue (chạy tiếp đến breakpoint tiếp theo):**
```csharp
🔴   9              var data = "Hello SimplCommerce";
     
     ⌨️ Nhấn F5 →
     
     → Website load tiếp, không pause (trừ khi gặp breakpoint khác)
```

---

### 🖼️ BƯỚC 6: DÙNG IMMEDIATE WINDOW

**Mở Immediate Window:**
```
⌨️ Nhấn: Ctrl + Alt + I
```
**Hoặc:** `Debug → Windows → Immediate`

**Nhìn thấy gì:**
```
┌─────────────────────────────────────────────────────────┐
│ Immediate                                     [x]       │
├─────────────────────────────────────────────────────────┤
│ >                                                       │ ← Gõ lệnh ở đây
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Ví dụ sử dụng:**
```csharp
> data
"Hello SimplCommerce"

> data.Length
19

> data.ToUpper()
"HELLO SIMPLCOMMERCE"

> HttpContext.Request.Path
"/"
```

---

### 🖼️ BƯỚC 7: STOP DEBUG

**Cách 1: Hotkey**
```
⌨️ Nhấn: Shift + F5
```

**Cách 2: Click Button**
```
Click: [⏹️ Stop] trên toolbar
```

**Kết quả:**
- Console đóng
- Code trở về normal mode (không còn breakpoint highlight)
- Browser có thể báo lỗi (bình thường)

---

## 🗄️ PHẦN 3: XEM DATABASE (GUI)

### 🖼️ BƯỚC 1: MỞ SQL SERVER OBJECT EXPLORER

**Cách 1: Menu**
```
View → SQL Server Object Explorer
```

**Cách 2: Hotkey**
```
⌨️ Nhấn: Ctrl + \, Ctrl + S
(Giữ Ctrl, nhấn \, rồi nhấn S)
```

**Nhìn thấy gì:**
```
┌─────────────────────────────────────────────────────────┐
│ SQL Server Object Explorer                    [x]       │
├─────────────────────────────────────────────────────────┤
│ 🔍 [Search...]                                          │
│                                                         │
│ ▶ SQL Server                                           │
│   ▶ (localdb)\MSSQLLocalDB (SQL Server 16)            │
│   ➕ Add SQL Server...                                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 🖼️ BƯỚC 2: EXPAND SERVER

**Click vào ▶ để expand:**
```
▼ SQL Server
  ▼ (localdb)\MSSQLLocalDB (SQL Server 16 - PC\User)
    ▶ Databases
    ▶ Security
    ▶ System Databases
```

**Expand Databases:**
```
▼ (localdb)\MSSQLLocalDB
  ▼ Databases
    📁 SimplCommerce  ← Database của bạn!
    📁 master
    📁 tempdb
```

**Expand SimplCommerce:**
```
▼ SimplCommerce
  ▼ Tables
    📊 dbo.Core_AppSetting
    📊 dbo.Core_Country
    📊 dbo.Core_District
    📊 dbo.Core_Role
    📊 dbo.Core_User  ← Click vào đây để xem!
    ... (80+ tables)
  ▶ Views
  ▶ Stored Procedures
  ▶ Security
```

---

### 🖼️ BƯỚC 3: XEM DATA TRONG TABLE

**Right-click table:**
```
📊 dbo.Core_User
  |
  └─ Right-click
     ┌─────────────────────────────┐
     │ View Data               ⌨️   │ ← CLICK!
     │ View Designer                │
     │ Script Table as              │
     │ ───────────────────────      │
     │ Refresh                      │
     │ Properties                   │
     └─────────────────────────────┘
```

**Data grid hiện ra:**
```
┌─────────────────────────────────────────────────────────────────┐
│ dbo.Core_User                                      [x]           │
├────┬──────────────────────────┬─────────────┬──────────────────┤
│ Id │ Email                    │ FullName    │ CreatedOn        │
├────┼──────────────────────────┼─────────────┼──────────────────┤
│  2 │ system@simplcommerce.com │ System User │ 2018-05-29...    │
│ 10 │ admin@simplcommerce.com  │ Shop Admin  │ 2018-05-29...    │
└────┴──────────────────────────┴─────────────┴──────────────────┘
        ↑ Scroll ngang để xem thêm columns
```

**Chỉnh sửa data:**
- Click vào cell → edit value
- Nhấn **Enter** → save changes

---

### 🖼️ BƯỚC 4: CHẠY SQL QUERY

**Right-click database SimplCommerce:**
```
📁 SimplCommerce
  |
  └─ Right-click
     ┌─────────────────────────────┐
     │ New Query...            ⌨️   │ ← CLICK!
     │ View Data                    │
     │ ───────────────────────      │
     │ Refresh                      │
     │ Properties                   │
     └─────────────────────────────┘
```

**Query window mở ra:**
```
┌─────────────────────────────────────────────────────────┐
│ SQLQuery1.sql                    [▶️ Execute]     [x]   │
├─────────────────────────────────────────────────────────┤
│  1  SELECT * FROM Core_User                             │
│  2  WHERE Email LIKE '%admin%'                          │
│  3                                                      │ ← Gõ query ở đây
└─────────────────────────────────────────────────────────┘
```

**Gõ query:**
```sql
-- Xem tất cả users
SELECT * FROM Core_User

-- Xem tất cả tables
SELECT TABLE_NAME 
FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_TYPE = 'BASE TABLE'
ORDER BY TABLE_NAME

-- Count products
SELECT COUNT(*) as TotalProducts
FROM Catalog_Product

-- Xem payment providers
SELECT Id, Name, IsEnabled, AdditionalSettings
FROM Payments_PaymentProvider
```

**Execute query:**
```
⌨️ Nhấn: Ctrl + Shift + E
```
**Hoặc:** Click nút **[▶️ Execute]**

**Kết quả hiện dưới:**
```
┌─────────────────────────────────────────────────────────┐
│ Results                               Messages          │
├─────────────────────────────────────────────────────────┤
│ TotalProducts                                           │
│ 0                                                       │
│                                                         │
│ (1 row affected)                                       │
└─────────────────────────────────────────────────────────┘
```

---

## ✅ CHECKLIST ĐẦY ĐỦ

### Lần đầu setup:
- [ ] Đã mở `SimplCommerce.sln`
- [ ] NuGet packages đã restore xong
- [ ] `SimplCommerce.WebHost` đã set as Startup Project (in đậm)
- [ ] File `appsettings.Development.json` đã có connection string đúng
- [ ] LocalDB đã start: `sqllocaldb start MSSQLLocalDB`
- [ ] Database migration xong (có 86 tables)

### Mỗi lần code:
- [ ] Mở Visual Studio
- [ ] Mở Solution `SimplCommerce.sln`
- [ ] Set breakpoints nếu cần debug
- [ ] Nhấn **F5** (debug) hoặc **Ctrl+F5** (run)
- [ ] Test features trên browser
- [ ] Check database nếu cần

---

## 🎯 SHORTCUTS QUAN TRỌNG NHẤT

| Phím | Chức năng |
|------|-----------|
| **Ctrl + F5** | Start Without Debugging (nhanh) |
| **F5** | Start With Debugging |
| **Shift + F5** | Stop Debugging |
| **F9** | Toggle Breakpoint |
| **F10** | Step Over (debug) |
| **F11** | Step Into (debug) |
| **Ctrl + \, Ctrl + S** | SQL Server Object Explorer |
| **Ctrl + Alt + L** | Solution Explorer |
| **Ctrl + Alt + I** | Immediate Window |
| **Ctrl + Shift + B** | Build Solution |

---

**Chúc bạn code vui vẻ! 🚀**

